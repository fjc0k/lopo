let x = 1

const func = () => {
  console.log(1)
  const { y: y0, z } = x
  const y = `${y0}2`
  return (x = 2 && x == null && x != null && y && z)
}

/*
 * hrllo
 * d
 */
const obj = {
  dd: 1,
  的多看看: '44',
  func: x
    ? 2
    : func
}

obj
  .dd
  .length()

export default x
