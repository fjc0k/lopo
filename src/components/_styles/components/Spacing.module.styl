@require '../settings';

for $size in (xs sm md lg xl) {
  .{$size} {
    &-top {
      margin-top: lookup('$v-spacing-' + $size);
    }
    &-bottom {
      margin-bottom: lookup('$v-spacing-' + $size);
    }
    &-left {
      margin-left: lookup('$h-spacing-' + $size);
    }
    &-right {
      margin-right: lookup('$h-spacing-' + $size);
    }
  }
}

.between-x {
  &.first-child {
    margin-left: 0;
  }
  &.last-child {
    margin-right: 0;
  }
}

.between-y {
  &.first-child {
    margin-top: 0;
  }
  &.last-child {
    margin-bottom: 0;
  }
}
