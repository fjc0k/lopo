@require '../settings';

.tip {
  &:global(.l-popover) {
    color: $fill-base;
    background-color: $tip-bg-color;
  }
  :global(.l-popover-arrow) {
    color: $tip-bg-color;
  }
}

.close {
  color: rgba(white, 40%);
  margin-left: $h-spacing-sm;
}
