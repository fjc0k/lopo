@require '../settings';

.preview {
  reset: base;
  user-select: auto;
  background: $fill-base;
  padding: $v-spacing-md 0 $v-spacing-md $h-spacing-lg;
  &:not(.no-border) {
    hairline(top bottom);
  }
}
