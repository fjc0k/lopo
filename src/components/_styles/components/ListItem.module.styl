@require '../settings';

.list-item {
  reset: font;
  display: flex;
  align-items: center;
  margin-left: $h-spacing-md;

  // 点击反馈
  &.tappable {
    &.active {
      background-color: $fill-tap;
    }
  }

  // 分隔线
  &.long-divider {
    margin-left: 0;
    padding-left: $h-spacing-md;
    &:not(:last-child) {
      hairline(bottom);
    }
  }
  &:not(.long-divider):not(.no-divider):not(:last-child) .body {
    hairline(bottom);
  }
}

// 图标
.icon {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  font-size: 21px;
}

// 主内容
.body {
  reset: box-model;
  flex-grow: 1;
  display: flex;
  align-items: center;
  min-height: 44px;
  overflow: hidden;
  padding: $h-spacing-md $h-spacing-md $h-spacing-md 0;

  // 左部内容
  .left {
    flex-shrink: 0;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    font-size: $font-size-caption;

    // 标题
    .title {
      flex-basis: 100%;
      overflow: hidden;
    }

    // 简介
    .desc {
      font-size: $font-size-base;
      flex-basis: 100%;
      overflow: hidden;
      color: $color-text-caption;
      line-height: 1.5;
    }
  }

  // 内容区
  .value {
    // 以极大的 grow 和 shrink 值令其自适应
    flex-grow: 2000;
    flex-shrink: 2000;
    overflow: hidden;
    font-size: $font-size-caption;
  }

  // 其他信息
  .extra {
    font-size: $font-size-base;
    color: $color-text-caption;
    text-wrap: true;
    overflow: hidden;
  }

  // 链接箭头
  .chevron {
    flex-shrink: 0;
    font-size: $font-size-base;
    color: $color-line-dark;
    margin-left: $h-spacing-xs;
  }
}

// 间隔
.icon + .body, .left + .value, .value + .extra {
  margin-left: $h-spacing-md;
}
